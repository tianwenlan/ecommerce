#!usr/local/python

import mysql.connector
import cgi, cgitb

print "hello world"

# db = mysql.connector.connect(host="localhost", port=3307, user="root", passwd="yours.", db="ecommerce")
# cursor = db.cursor()

# cursor.execute("SELECT * FROM products");

# if (!$con) echo 'Connected failed';

# if($con) echo 'Connected successfully';

# mysql_close($con);


